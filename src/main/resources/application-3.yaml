server:
  port: 7000

raft:
  servers:
    - "localhost:5001"
    - "localhost:6001"
    - "localhost:7001"
  self: "localhost:7001"
  electionTimeout: 15000
  heartBeatFrequent: 5000
  log: "log3.json"

library:
  address: "library3"

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
